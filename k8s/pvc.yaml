apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rag-ingest-data
  labels:
    app: rag-ingest
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # storageClassName: standard  # set if your cluster needs it
